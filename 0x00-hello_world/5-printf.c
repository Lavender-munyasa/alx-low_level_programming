#include<stdio.h>

/**
 * main - a program that writes a string
 *
 * description -using printf
 *
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
